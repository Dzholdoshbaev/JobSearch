server:
  port: 8089
spring:
  application:
    name: JobSearch

  datasource:
    url: jdbc:h2:./db/jobSearch;AUTO_SERVER=TRUE
    username: admin
    password: 12345
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always